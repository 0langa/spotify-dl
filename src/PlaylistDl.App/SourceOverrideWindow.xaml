<Window x:Class="PlaylistDl.App.SourceOverrideWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choose audio source" Height="245" Width="620" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Background="#181B22">
  <Grid Margin="22">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <TextBlock x:Name="TrackTitle" FontSize="18" FontWeight="SemiBold" Foreground="White" TextWrapping="Wrap" />
    <TextBlock Grid.Row="1" Margin="0,7,0,14" Foreground="#AEB6C5"
               Text="Paste the exact YouTube or YouTube Music video URL to use. Leave blank for automatic matching." />
    <TextBox Grid.Row="2" x:Name="SourceUrlBox" VerticalContentAlignment="Center" />
    <TextBlock Grid.Row="3" x:Name="ValidationText" Margin="0,7,0,0" Foreground="#FF8D8D" />
    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Cancel" Width="90" Margin="0,0,10,0" IsCancel="True" />
      <Button Content="Save source" Width="110" Background="#4F7DFF" Foreground="White" Click="SaveButton_Click" />
    </StackPanel>
  </Grid>
</Window>
