<Window x:Class="PlaylistDl.App.RunLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Playlist DL run log" Height="650" Width="980" MinHeight="400" MinWidth="640"
        WindowStartupLocation="CenterOwner" Background="#181B22">
  <Grid Margin="22">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <TextBlock Text="Run log" FontSize="26" FontWeight="SemiBold" Foreground="White" />
    <TextBox Grid.Row="1" x:Name="LogPathBox" Margin="0,10,0,10" IsReadOnly="True"
             ToolTip="Exact location of this app session's retained log" />
    <TextBox Grid.Row="2" x:Name="LogTextBox" IsReadOnly="True" AcceptsReturn="True"
             TextWrapping="NoWrap" FontFamily="Consolas" FontSize="13"
             HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
             AutomationProperties.Name="Run log contents" />
    <DockPanel Grid.Row="3" Margin="0,12,0,0" LastChildFill="False">
      <TextBlock x:Name="StatusText" DockPanel.Dock="Left" Foreground="#AEB6C5"
                 VerticalAlignment="Center" />
      <Button DockPanel.Dock="Right" Content="Close" Width="90" IsCancel="True" />
      <Button DockPanel.Dock="Right" Content="Copy all" Width="100" Margin="0,0,10,0"
              Click="CopyButton_Click" />
      <Button DockPanel.Dock="Right" Content="Refresh" Width="90" Margin="0,0,10,0"
              Click="RefreshButton_Click" />
    </DockPanel>
  </Grid>
</Window>
